# EEG Adaptive EQ — Python dependencies
# Tested on Python 3.11 / Windows 11 / anaconda3

# GUI
PyQt5>=5.15
pyqtgraph>=0.13

# Numeric / signal processing
numpy>=1.24
scipy>=1.11

# Audio I/O & DSP
sounddevice>=0.4
pedalboard>=0.9

# OSC (Mind Monitor 受信)
python-osc>=1.8

# Video background (Emotional Seascape)
opencv-python>=4.8

# Optional: 3D widgets (band power gauges 等は 2D で十分)
# PyOpenGL>=3.1
# pywavelets>=1.4  # 旧 Wavelet Scalogram (現状不要)

# 画像処理 (アセットの crop/preview に使用)
Pillow>=10.0
